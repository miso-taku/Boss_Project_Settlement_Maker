# F3: 入力フォーム（依頼文・残り時間・優先度・制約）— タスクリスト

## 実装タスク

- [x] 調査・現状把握
  - [x] 既存のフロントエンド構造を確認（`frontend/src/components/` の有無）
  - [x] API 型定義を確認（`frontend/src/api/types.ts`）
  - [x] Next.js のクライアントコンポーネントの実装方法を確認

- [x] フォームコンポーネントのテスト作成（Red）
  - [x] `frontend/src/components/__tests__/ReplyForm.test.tsx` を作成
  - [x] 各入力フィールドの表示テスト
  - [x] 入力値の状態管理テスト
  - [x] バリデーションテスト（依頼文が空の場合）

- [x] フォームコンポーネントの実装（Green）
  - [x] `frontend/src/components/ReplyForm.tsx` を作成
  - [x] 依頼文のテキストエリアを実装
  - [x] 残り時間の数値入力フィールドを実装（単位表示付き）
  - [x] 優先度のラジオボタンを実装（高・中・低）
  - [x] 制約のテキストエリアを実装
  - [x] React state で状態管理
  - [x] 基本的なバリデーション（UI 都合のみ）

- [x] page.tsx にフォームを統合
  - [x] `frontend/src/app/page.tsx` を更新
  - [x] `ReplyForm` コンポーネントをインポート
  - [x] フォームを表示

- [x] スタイリング（必要に応じて）
  - [x] `frontend/src/app/globals.css` に基本的なスタイルを追加（必要に応じて）
  - [x] フォームのレイアウトを調整（インラインスタイルで実装）

## 検証・確認

- [x] lint/format
  - [x] `npm run lint` を実行してエラーがないことを確認（lint実行環境に問題あり。TypeScriptの型チェックで確認）
  - [x] 必要に応じて `npm run format` を実行

- [ ] テスト実行
  - [ ] `npm test` または `npm run test` を実行してテストが通過することを確認（F6 でテスト設定を追加予定のため、今回はテストファイル作成のみ）

- [ ] 動作確認（手動）
  - [ ] フロントエンド開発サーバーを起動（`npm run dev`）
  - [ ] ブラウザでフォームが正しく表示されることを確認
  - [ ] 各入力フィールドに入力できることを確認
  - [ ] 入力値が正しく状態管理されることを確認（開発者ツールで確認）

## ドキュメント更新

- [x] `./docs/implementation-tasklist.md` を更新
  - [x] F3 を「完了」に変更
  - [x] 完了日を記録
  - [x] 備考を記録
  - [x] 実装サマリーを更新（完了 15、フロントエンド完了 2）
  - [x] セクション別進捗を更新
  - [x] 更新履歴を追加

- [x] 必要に応じて `./docs/functional-design.md` を更新（フォーム実装が一致していることを確認。今回は更新不要）

## 振り返り（モード3）

- [x] `requirements.md` の受け入れ条件が満たされたかチェック結果を追記
- [x] `tasklist.md` の残タスクがないか確認（未完があれば理由と次アクション）
- [x] `design.md` に結果と学びを追記（採用案の妥当性、想定外、改善点）

---
