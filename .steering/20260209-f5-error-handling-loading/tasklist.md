# F5: エラーハンドリング・ローディング表示 — タスクリスト

## 実装タスク

- [x] 調査・現状把握
  - [x] 既存の `ReplyForm` コンポーネントのエラーハンドリング・ローディング表示を確認
  - [x] API クライアント関数（`generateReplyDrafts`）のエラーハンドリングを確認
  - [x] FastAPI のエラーレスポンス形式を確認（バックエンドのエラーハンドリング）

- [x] エラー型定義の追加
  - [x] `frontend/src/api/types.ts` にエラー型（`ApiError` 等）を追加
  - [x] エラータイプ（HTTPエラー、ネットワークエラー、バリデーションエラー等）を定義

- [x] API クライアントのエラーハンドリング改善（Green）
  - [x] `frontend/src/api/replyDrafts.ts` を更新
  - [x] HTTPステータスコード別のエラー処理を実装
  - [x] ネットワークエラーの適切な処理を実装
  - [x] エラーレスポンスの詳細情報を取得し、適切なエラーオブジェクトを返す
  - [x] `ApiErrorException` クラスを追加（後方互換性のため）

- [x] ローディング表示コンポーネントの実装（Green）
  - [x] スピナーコンポーネントを実装（CSS アニメーション）
  - [x] `ReplyForm` コンポーネントにスピナーを統合
  - [x] ローディング状態の明確な表示

- [x] エラー表示の改善（Green）
  - [x] `ReplyForm` コンポーネントのエラー表示を改善
  - [x] HTTPステータスコード別のエラーメッセージを実装
  - [x] ネットワークエラー時のユーザーフレンドリーなメッセージを実装
  - [x] バリデーションエラー時の詳細なエラーメッセージを実装
  - [x] エラー表示のUI改善（見やすさ、アクセシビリティ）

- [x] コンポーネントテスト作成（Red）
  - [x] `frontend/src/components/__tests__/ReplyForm.test.tsx` を更新
  - [x] ローディング表示のテスト
  - [x] HTTP エラー時のエラー表示のテスト
  - [x] ネットワークエラー時のエラー表示のテスト
  - [x] バリデーションエラー時のエラー表示のテスト

- [x] API クライアントテスト作成（Red）
  - [x] `frontend/src/api/__tests__/replyDrafts.test.ts` を更新
  - [x] HTTP エラー時のエラー処理のテスト
  - [x] ネットワークエラー時のエラー処理のテスト
  - [x] バリデーションエラー（配列形式）のテスト

- [x] リファクタ（Refactor）
  - [x] 重複コードの除去
  - [x] 命名の改善
  - [x] エラーハンドリングロジックの整理

## 検証・確認

- [x] lint/format
  - [x] `npm run lint` を実行してエラーがないことを確認（lint エラーなし）
  - [x] TypeScript の型チェックを確認（tsconfig.json の設定は Next.js 標準設定のため、next build で確認予定）

- [ ] テスト実行
  - [ ] `npm test` または `npm run test` を実行してテストが通過することを確認（F6 でテスト設定を追加予定のため、今回はテストファイル作成のみ）

- [ ] 動作確認（手動）
  - [ ] フロントエンド開発サーバーを起動（`npm run dev`）
  - [ ] バックエンド開発サーバーを起動（`uv run uvicorn ...`）
  - [ ] 正常系: API 呼び出し中にスピナーが表示されることを確認
  - [ ] 正常系: API 呼び出し完了時にスピナーが非表示になることを確認
  - [ ] 異常系: HTTP エラー（400, 404, 500等）時に適切なエラーメッセージが表示されることを確認
  - [ ] 異常系: ネットワークエラー時にユーザーフレンドリーなメッセージが表示されることを確認
  - [ ] 異常系: バリデーションエラー時に詳細なエラーメッセージが表示されることを確認

## ドキュメント更新

- [x] `./docs/implementation-tasklist.md` を更新
  - [x] F5 を「完了」に変更
  - [x] 完了日を記録（2026-02-09）
  - [x] 備考を記録
  - [x] 実装サマリーを更新（完了 17、フロントエンド完了 5）
  - [x] セクション別進捗を更新（フロントエンド 71%）
  - [x] 更新履歴を追加

- [x] 必要に応じて `./docs/functional-design.md` を更新（エラーハンドリング・ローディング表示の実装が一致していることを確認。今回は更新不要）

## 振り返り（モード3）

- [x] `requirements.md` の受け入れ条件が満たされたかチェック結果を追記
- [x] `tasklist.md` の残タスクがないか確認（未完があれば理由と次アクション）
- [x] `design.md` に結果と学びを追記（採用案の妥当性、想定外、改善点）

---
