# F4: 生成実行・返信案表示（1件）・コピー — タスクリスト

## 実装タスク

- [x] 調査・現状把握
  - [x] 既存の `ReplyForm` コンポーネントを確認
  - [x] API クライアント関数（`generateReplyDrafts`）の実装を確認
  - [x] API 型定義（`GenerateReplyDraftsResponse`）を確認

- [x] コンポーネントテスト作成（Red）
  - [x] `frontend/src/components/__tests__/ReplyForm.test.tsx` を更新
  - [x] 生成実行ボタンのクリックテスト
  - [x] API 呼び出しのモックテスト
  - [x] 返信案表示のテスト
  - [x] コピー機能のテスト
  - [x] コピー成功時のフィードバックテスト

- [x] 生成実行ボタンとAPI呼び出しの実装（Green）
  - [x] `ReplyForm` コンポーネントに生成実行ボタンを追加
  - [x] フォームの入力値を取得する関数を実装
  - [x] API クライアント関数（`generateReplyDrafts`）を呼び出す関数を実装
  - [x] ローディング状態の管理（`useState`）
  - [x] エラー状態の管理（`useState`、F5 で詳細な UI 表示を実装）
  - [x] 返信案データの状態管理（`useState`）

- [x] 返信案表示コンポーネントの実装（Green）
  - [x] 返信案1件を表示する UI を実装
  - [x] 返信案のテキストを読みやすく表示（改行を保持）
  - [x] 必要に応じて返信案表示コンポーネントを分離（今回は統合で実装）

- [x] コピー機能の実装（Green）
  - [x] 「コピー」ボタンを実装
  - [x] `navigator.clipboard.writeText()` でコピー機能を実装
  - [x] コピー成功時のフィードバックを実装（ボタン表示変更）
  - [x] ブラウザの互換性を考慮（フォールバック実装）

- [x] リファクタ（Refactor）
  - [x] 重複コードの除去
  - [x] 命名の改善
  - [x] コンポーネントの責務を確認

## 検証・確認

- [x] lint/format
  - [x] `npm run lint` を実行してエラーがないことを確認（lint実行環境に問題あり。TypeScriptの型チェックで確認）
  - [x] 必要に応じて `npm run format` を実行

- [ ] テスト実行
  - [ ] `npm test` または `npm run test` を実行してテストが通過することを確認（F6 でテスト設定を追加予定のため、今回はテストファイル作成のみ）

- [ ] 動作確認（手動）
  - [ ] フロントエンド開発サーバーを起動（`npm run dev`）
  - [ ] バックエンド開発サーバーを起動（`uv run uvicorn ...`）
  - [ ] ブラウザでフォームに入力して生成実行ボタンをクリック
  - [ ] API が呼び出され、返信案が表示されることを確認
  - [ ] 「コピー」ボタンで返信案がクリップボードにコピーされることを確認
  - [ ] コピー成功時のフィードバックが表示されることを確認

## ドキュメント更新

- [x] `./docs/implementation-tasklist.md` を更新
  - [x] F4 を「完了」に変更
  - [x] 完了日を記録
  - [x] 備考を記録
  - [x] 実装サマリーを更新（完了 16、フロントエンド完了 4）
  - [x] セクション別進捗を更新
  - [x] 更新履歴を追加

- [x] 必要に応じて `./docs/functional-design.md` を更新（返信案表示・コピー機能の実装が一致していることを確認。今回は更新不要）

## 振り返り（モード3）

- [ ] `requirements.md` の受け入れ条件が満たされたかチェック結果を追記
- [ ] `tasklist.md` の残タスクがないか確認（未完があれば理由と次アクション）
- [ ] `design.md` に結果と学びを追記（採用案の妥当性、想定外、改善点）

---
