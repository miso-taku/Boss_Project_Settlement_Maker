# settlement-maker（バックエンド）

上司案件・落とし所AIエージェントの返信案生成API（FastAPI + PydanticAI）。

## セットアップ

```bash
uv sync
```

## 実行

```bash
uv run uvicorn settlement_maker.interface.app:app --reload
```

（アプリ起動エントリは B6 以降で追加予定）
